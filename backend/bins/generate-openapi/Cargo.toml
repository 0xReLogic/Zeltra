[package]
name = "generate-openapi"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Utility to generate OpenAPI specification"
publish = false

[[bin]]
name = "generate-openapi"
path = "src/main.rs"

[dependencies]
zeltra-api = { path = "../../crates/api" }
utoipa = { workspace = true }
serde_yaml = "0.9"
serde_json = "1.0"

[lints]
workspace = true
